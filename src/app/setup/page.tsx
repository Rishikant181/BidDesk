import Link from "next/link";
export default function Setup(){return <main className="setup"><div className="brandmark">B</div><p className="eyebrow">BIDDESK · LOCAL SETUP</p><h1>A workspace ready<br/>for your next bid.</h1><p>Add your Atlas connection to <code>.env.local</code>, along with a database name and authentication secret. Restart the app after changing configuration.</p><pre>MONGODB_URI=your-atlas-connection-string{"\n"}MONGODB_DB=biddesk{"\n"}BETTER_AUTH_SECRET=at-least-32-random-characters{"\n"}BETTER_AUTH_URL=http://localhost:3000</pre><p>Then run <code>npm run check:config</code>, <code>npm run db:indexes</code>, and the import command in the README. No sample database is substituted.</p><Link className="button" href="/overview">Open workspace →</Link></main>;}
